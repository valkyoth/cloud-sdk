//! Label and label-selector domains.

/// Maximum label key length accepted by the SDK policy until source-locked.
pub const MAX_LABEL_KEY_LEN: usize = 63;

/// Maximum label value length accepted by the SDK policy until source-locked.
pub const MAX_LABEL_VALUE_LEN: usize = 63;
