[package]
name = "cloud-sdk-sanitization"
description = "Provider-neutral mandatory cleanup and secret-sanitization boundary for cloud-sdk."
version = "0.18.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true
readme = "README.md"

[package.metadata.docs.rs]
all-features = true

[features]
default = []
alloc = ["sanitization/alloc"]
std = ["alloc"]

[dependencies]
sanitization.workspace = true

[lints]
workspace = true
