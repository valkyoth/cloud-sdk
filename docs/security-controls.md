# Security Controls

| Control | Status |
| --- | --- |
| no_std default graph | active |
| unsafe forbidden | active |
| provider/service identity | allocation-free bounded canonical IDs; lowercase ASCII/digit syntax with single internal hyphens; private representation; provider-owned open markers; service markers require an associated provider; external-provider and compile-fail compatibility evidence; no central provider registry |
| default transport dependency | none |
| default token storage | none |
| dependency policy | active |
| SBOM generation | scripted for the production workspace, reqwest feature-unification fixture, and fuzz tooling graph; cargo-sbom output is completed from locked Cargo metadata; independent package completeness and canonical freshness checks are required in CI and release gates |
| cargo audit | required before tags |
| cargo deny | required before tags |
| fuzzing | 33 isolated non-published libFuzzer targets; pinned nightly and cargo-fuzz; synthetic tracked seeds; dedicated neutral transport, response, pagination, retry, and every implemented Robot response-family boundary; named positive seeds receive deterministic success-path preflight; temporary writable smoke corpora; target-specific limits include exact rejection boundaries; deterministic regressions remain authoritative |
| release security cadence | stable releases require the complete automated gate, explicit security review, pentest evidence, and green GitHub CI and CodeQL; v1.0.0 receives a full-project assessment against the published v0.100.0 candidate |
| pentest content binding | every report binds the immediately preceding tag, exact range end, and reviewed implementation commit; the reviewed commit must remain an ancestor of the final GitHub-validated release commit, and public checkpoints verify the complete intervening report chain |
| pentest provenance | every tag requires a committed PASS report with assessment, baseline, range end, reviewed commit, tester, scope, and date fields |
| release publishing | intermediate stages mechanically select no crates and the publisher refuses to run; public readiness and the complete release gate require one clean unchanged `HEAD` at entry and exit; accumulated package-tree checks reject lost changes; signed annotated tag must verify; publisher captures and revalidates the clean commit, tag target, and signature before every locked crate publication; no normal-path bypass flags |
| OpenAPI integrity | exact non-redirecting HTTPS source URL with default certificate and hostname verification; full pinned raw SHA-256 before parsing or one reviewed full-document set-order normalization before hashing; size/time ceilings and no-follow descriptor reads for local inputs |
| public IPv6 targets | conservative IANA allocation allowlist pinned in `docs/IANA_IPV6_SOURCE_LOCK.md`; live registry drift uses exact non-redirecting HTTPS URLs, bounded downloads, and digest-before-parse authentication |
| request encoding | immutable snapshots are measured with checked arithmetic, aggregate caps, written only after exact capacity admission, and replayed against exact output; undersized buffers remain unchanged, while drift, write errors, verification errors, and panic unwinding clear only the admitted region |
| secret buffer failure | path, query, header, and JSON writes preflight capacity and leave undersized buffers unchanged |
| secret buffer cleanup | `cloud-sdk-sanitization::SecretBuffer` volatile-clears the full caller-owned destination on drop; alloc-backed `SecretString` consumes owned UTF-8 without another plaintext copy, clears full allocation capacity on drop, wipes before growth, and permits only closure-scoped UTF-8 access |
| request preparation cleanup | `PreparationStorageGuard` ties prepared-request lifetime to one non-`Copy` owner, clears complete target and body storage before every preparation attempt and again on drop, including tails; profile validation establishes cleanup ownership before either capacity can fail; embedded/default/large profiles are bounded and owned allocation is explicit and fallible |
| response workspace cleanup | core mandates the single admitted volatile primitive for complete caller-owned body and header storage, temporary content type, request identifiers, cursor/provider-link staging, decoder scratch, and lifecycle scalars; sensitive header bytes remain at a stable address while movable state contains only non-secret locators; additive platform hooks run between mandatory body clears and cannot replace the final clear |
| provider-generic client kernel | allocation-free blocking, Send-async, and local-async path joins typed preparation, endpoint/authentication checks, one transport attempt, checked success/error decoding, and cleanup; each in-flight request consumes one caller-owned four-buffer lease from fixed atomic admission; exhaustion is immediate with no hidden queue; mutable regions remain uniquely borrowed across await and clear before slot release; decoders receive no reusable prepared request; direct state-changing and cost-bearing execution remains permit-gated |
| retained response metadata | request-ID policy is explicit per operation (`Retain`, `Protected`, or `Discard`); retained state wraps stable caller-owned destination storage, is non-`Copy` and closure-accessed, and copies directly from stable header storage with source cleanup on success or failure |
| private-key output | escaped atomic writer only; no raw accessor or ordinary equality; guarded cleanup tested |
| DNS TSIG policy | HMAC-SHA256 only; canonical Base64; minimum 32 decoded bytes; no ordinary equality on secret-bearing types |
| DNS RRSet mutations | source-locked RR types; bounded unique redacted records; mandatory change-TTL intent; atomic JSON-string writers |
| optional Serde boundary | default graph exclusion; no Serde or serde_json `std`; 1 MiB request and 8 MiB response policies; direct duplicate-rejecting checked-response parser with a shared 65,536-node budget, bounded depth/containers/strings, and volatile-clearing decode storage for escaped and unescaped string values and object keys, including ignored extensions; protected provider/action error messages; source-locked checked decoding for every active operation; typed actions, resource identities, pagination, special results, and provider errors |
| testkit boundary | no_std ordered and dynamic mocks; atomic bounded response writes and caller-owned payload-free records; non-consuming selection/staging failures; finite validated pagination/action scripts; hard-limit stream patterns and exact injected faults; payload-free errors and redacted fixture/request debug |
| transport contract | shared-reference blocking plus executor-neutral async traits; response mutation and commitment are private outside cleanup-owning attempts; attempts clear prior residue on entry and complete uncommitted body/header storage on error, unwind, timeout, or cancellation; prepared execution mandates complete response-storage sanitization before endpoint verification and capacity admission; normalized credential-free endpoint identity; exact Hetzner official-endpoint verifier derived from the canonical base-URL constants for both v1 API families; origin-form targets reject leading `//`, backslash, fragments, controls, spaces, and non-ASCII; responses borrow their initialized caller-buffer slice; core preserves absent versus malformed content type and rejects every present parse failure before provider decoding; no independent untrusted body length; no authentication, headers, TLS, retry, runtime, or network implementation |
| authentication policy | type-separated bearer and Basic credentials; mandatory provider/service/normalized-HTTPS-endpoint binding; exact `Required` base-identity policy plus provider or operation-owned `Required`, `Optional`, or `Forbidden` audience/account/tenant rules; configured endpoint mismatch, base-rule downgrade, omission, forbidden presence, and mismatch fail before credential snapshot or header construction; bearer refresh handoffs bind to one lineage and generation; Basic delimiters/charset/length are bounded; authenticated clients do not implement policy-free transport traits |
| canonical signing input | v2 bounded length framing over provider, service, normalized endpoint with tagged DNS/IPv4/IPv6 identity, optional scope, key ID, distinct digest and signature algorithms, exact method/target, selected headers, internally coupled exact-body digest, nonce, and time; equivalent IPv6 spellings encode identically; body hashers report and must match the signed digest algorithm; retains the exact request; validates signer output; digest, canonical, and signature storage clear on failure, unwind, or drop; cryptography, keys, clocks, randomness, replay, and verification remain caller/provider policy |
| optional blocking transport | cloneable shared non-default reqwest/rustls client; HTTPS only; TLS 1.2 minimum; HTTP/1 and system DNS forced under feature unification; explicit bounded timeouts and user agent; no redirect, retry, proxy, referer, decompression, queue, or background worker; exact response bounds; payload-free failures |
| FIPS deferment | absent from active features, dependencies, lockfiles, transport source, packages, and runtime claims; CI rejects reintroduction; any future optional support waits for Brynja and exact module, certificate, operating-environment, build, runtime, and pentest qualification |
| optional async transport | cloneable shared non-default reqwest/rustls client with caller-provided Tokio execution; blocking feature excluded from async-only graph; no credential lock across `.await`; complete-success response copy from bounded sanitized temporary storage; cancellation, timeout, read failure, and overflow leave caller output cleared |
| quota and retry metadata | transports retain only bounded provider-admitted headers and reject duplicates; provider decoders reject malformed, partial, zero-limit, overflow, and incoherent buckets; core separates duration, wall-clock, and HTTP-date values and returns only pure caller-maximum-bound decisions with explicit stale, rollback, unknown-reset, and conflict policy |
| pagination state | distinct no_std numbered, offset, cursor, marker, and provider-link strategies; hard request/item/state/history budgets; exact bounded snapshot identities; transactional progression; cleanup-owning non-Copy opaque state; exact cursor history with cycle and digest-collision rejection; raw provider links retain endpoint/method/operation/exact-path identity, couple endpoint verification to blocking or async authenticated execution through the same transport object, flatten validation and transport failures into one result, redact transport-error diagnostics, and remain barred from structured recomposition; drift, contradiction, empty continuation, unbound transport, authority/scheme/userinfo/fragment/path/method/operation changes rejected |
| workflow drivers | no_std non-cloneable pager and action owners; exactly one response per admitted request; pagination retains transactional request/item/state/snapshot budgets; action polling has unconditional observation, per-delay, cumulative-delay, and monotonic elapsed bounds; cancellation and backoff are separate; progress regression requires an explicit bounded reset policy; provider wall-clock telemetry cannot extend local budgets; generic policy and provider failures have redacted Debug; caller owns clocks, sleep, transport, and execution |
| live smoke harness | ignored by default; repository-anchored clean-commit staging without Cloud or Robot credential variables; privileged installation into root-owned non-writable paths; manifest-bound Cloud and Robot launchers; isolated root-owned runtime validates ownership, modes, link count, bounded manifest, and open-descriptor SHA-256 before descriptor execution; authenticated phase invokes no Cargo/build tooling; exact fixed mode and official origin; Cloud private bearer file or separate private Robot username/password files; bounded preallocated reads and guarded cleanup; Robot files use Unix descriptor-level no-follow/close-on-exec opens with effective-user owner, private-parent, single-link, and owner-only mode checks, while non-Unix live loading fails closed; typed GET-only Cloud probes and one typed Robot `GET /server`; the shared Robot execution function is exercised by an exact-match authenticated transport that rejects method, target, body, header, endpoint, and extra-dispatch drift; substring inspection is secondary only; no invalid-login test, retry, mutation, order, custom endpoint, or CI execution; static diagnostics without credential paths, bodies, or IDs; destructive mode rejected |
| mutation safety qualification | credential-free CI gate selects exact typed Cloud, DNS, Security, Console Storage, Robot mutation, and Robot cost/reconciliation tests; the gate rejects credential and destructive-opt-in variables, performs no network request, and never dispatches a Robot purchase; real mutation qualification and mandatory operational attestations are deferred to a separately reviewed future boundary |
| content-type diagnostics | validated values remain available to the adapter but all `Debug` output is structurally redacted |
| adapter secret ownership | immutable credential scope cannot change during token rotation; mutable and guarded bearer/Basic sources clear on success/rejection; Basic intermediate and encoded bytes clear; monotonic generations and compare-and-swap refresh reject stale completion; atomic bearer rotation preserves in-flight snapshots, recovers complete state after lock poisoning, and clears retired adapter-owned storage after last use; authorization headers have cleanup-owning allocations; credential and request-body copies are redacted and cleared through `cloud-sdk-sanitization`; immutable caller and reqwest/TLS/OS copies remain caller/operational boundaries |
| Robot source locks | credential-free, non-executing fixtures outside publishable crates; exact official-document digest; the narrow `v0.42.0` wire lock covers Basic/form/repeated-field/error/quota/maintenance/lockout/empty-body distinctions, while the complete `v0.74.0` operation lock binds all 105 official headings, 89 active operations, 16 excluded deprecated Storage Box operations, protocol semantics, and implementation ownership |
| Robot form codec | ordered duplicate-preserving borrowed fields; reviewed name grammar; exact standard form byte rules; checked field/count/body bounds; immutable measure/write/verify passes; validation and capacity rejection preserve output; complete destination clears before admitted writes and on encoded-body drop; value diagnostics always redacted; no credentials, endpoint, network, retry, or response behavior |
| Robot credential lockout | Robot-only provider/service/exact-origin scope; protected non-cloneable username/password owner under `alloc`; mutable and guarded source cleanup; closure-scoped non-escaping access; allocation-free core attempts borrow owner identity while Robot attempts retain a non-hashable opaque shared lineage; owner validates before generation/status; per-`RobotClient` shared atomic open/rejected generation plus exclusive dispatch admission; foreign-owner, stale-transition, overlap, and wrap rejection; unclassified drop/cancellation and indeterminate post-dispatch failure close the generation; final status survives later response-processing failure; replacement or explicit post-rejection reconfirmation only; independently constructed clients/processes require an external credential-keyed coordinator; no automatic authentication retry |
| Robot error and quota protocol | admitted `TransportResponse` only; 64 KiB body bound; JSON required for bodyful errors; duplicate/extra-field and status/body/code mismatch rejection; protected provider messages and input names; finite 400 invalid-input, 401 authentication, 403 quota, 404 provider, and 503 maintenance classes; unknown status/code fails closed; authentication retry denied; quota maps to an exhausted neutral bucket; transient transport construction requires a separate delivery-classified adapter failure |
| Robot cancellations | nine source-locked server/IP/subnet GET, POST, and DELETE operations; canonical protected path identity and dates; sensitive bounded forms; destructive permit metadata and retry denial; direct/shared request-bound permit execution across all transport modes; target-specific exact JSON or server-empty success policy; exact request/response association; complete schedule/reason/reservation availability/reserved acknowledgement and inactive revoke binding; shared Unicode display-safety policy; allocation-aware date decoding; canonical subnet enforcement; documented date-field spelling variants admitted exclusively |
| Robot IP management | six source-locked list/detail/update and separate-MAC operations; canonical protected address and lowercase EUI-48 identity; bounded duplicate-free assignment, lock, traffic, network, and nullable-MAC models; non-empty sensitive partial forms; exact request/response and optional server-filter association; direct/shared request-bound mutation/destructive permits across all transport modes; digest-required sensitive plans; mutation acknowledgement, network consistency, and MAC-state conflict rejection; preparation and unpolled-attempt cleanup; automatic retry denied except the explicitly policy-gated idempotent traffic update |
| CodeQL default setup | repository setting |
| API source lock | active for `v0.2.0` |
| provider-generic drift evidence | strict declarative plugins and bounded no-follow JSON; exact credential-free HTTPS sources; at most eight unique globally routable TCP destinations carried from one DNS answer into shared-deadline TLS setup with original-host SNI/certificate/Host verification and normal HTTP I/O timeout restoration; no proxies or redirects; aggregate admission; digest-before-observation verification; canonical payload-free diffs; explicit ownership/severity; and a Hetzner compatibility bridge to every authoritative operation/schema/policy lock |
| unpublished OVHcloud probe | exact `publish = false` workspace harness; five exact official sources and ten production authenticated GET candidates; source-derived authority, OAuth, schema, cursor, and task facts plus fixture-only generic event models; blocking/Send-async/local-async/testkit execution; ignored least-privilege live read; no release-plan package, publish-order entry, mutation, or provider support claim |
| Storage Boxes drift check | active for `v0.2.0` |
