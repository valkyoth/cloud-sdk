[release]
version = "1.1.0"
milestone = "1.1.0"
baseline = "1.0.0"
review_baseline = "1.0.0"
cumulative_milestones = ["1.1.0"]
policy = "independent"
stage = "candidate"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "1.0.0"
version = "1.1.0"
change = "metadata"
publish = false
reason = "Track the unreleased crates.io provider train without enabling publication."

[crates."cloud-sdk-hetzner"]
previous_version = "1.0.0"
version = "1.1.0"
change = "metadata"
publish = false
reason = "Keep the stable Hetzner provider on the coherent unreleased workspace version."

[crates."cloud-sdk-reqwest"]
previous_version = "1.0.0"
version = "1.1.0"
change = "metadata"
publish = false
reason = "Keep the neutral transport boundary on the coherent unreleased workspace version."

[crates."cloud-sdk-sanitization"]
previous_version = "1.0.0"
version = "1.1.0"
change = "metadata"
publish = false
reason = "Keep the cleanup boundary on the coherent unreleased workspace version."

[crates."cloud-sdk-testkit"]
previous_version = "1.0.0"
version = "1.1.0"
change = "metadata"
publish = false
reason = "Keep the testkit on the coherent unreleased workspace version."
