[release]
version = "0.42.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.41.0"
version = "0.42.0"
change = "code"
publish = true
reason = "Add bounded provider-neutral canonical signing input contracts."

[crates."cloud-sdk-hetzner"]
previous_version = "0.32.2"
version = "0.32.3"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.42 facade line."

[crates."cloud-sdk-reqwest"]
previous_version = "0.28.0"
version = "0.29.0"
change = "code"
publish = true
reason = "Add scoped Basic credentials and blocking and async authenticated clients."

[crates."cloud-sdk-sanitization"]
previous_version = "0.16.0"
version = "0.16.0"
change = "unchanged"
publish = false
reason = "No code, dependency, or package metadata changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.24.1"
version = "0.24.2"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.42 facade line."
