[release]
version = "0.13.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.12.0"
version = "0.13.0"
change = "metadata"
publish = true
reason = "README updates and v0.13.0 release evidence for the provider-neutral facade."

[crates."cloud-sdk-hetzner"]
previous_version = "0.12.0"
version = "0.13.0"
change = "code"
publish = true
reason = "No_std DNS RRSet CRUD, record mutation, TTL, and protection request domains."

[crates."cloud-sdk-reqwest"]
previous_version = "0.12.0"
version = "0.12.1"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.13 facade line."

[crates."cloud-sdk-sanitization"]
previous_version = "0.12.0"
version = "0.12.1"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.13 facade line."

[crates."cloud-sdk-testkit"]
previous_version = "0.12.0"
version = "0.12.1"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.13 facade line."
