[release]
version = "0.8.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.7.0"
version = "0.8.0"
change = "code"
publish = true
reason = "Release metadata, README updates, and v0.8.0 release evidence for the provider-neutral facade."

[crates."cloud-sdk-hetzner"]
previous_version = "0.7.0"
version = "0.8.0"
change = "code"
publish = true
reason = "No_std storage/IP request domains for volumes, floating IPs, and source-locked action endpoint paths."

[crates."cloud-sdk-hetzner-reqwest"]
previous_version = "0.7.0"
version = "0.8.0"
change = "metadata"
publish = true
reason = "Keep optional transport boundary metadata aligned with v0.8.0 release evidence."

[crates."cloud-sdk-hetzner-sanitization"]
previous_version = "0.7.0"
version = "0.8.0"
change = "metadata"
publish = true
reason = "Keep sanitization boundary metadata aligned with v0.8.0 release evidence."

[crates."cloud-sdk-hetzner-testkit"]
previous_version = "0.7.0"
version = "0.8.0"
change = "metadata"
publish = true
reason = "Keep testkit boundary metadata aligned with v0.8.0 release evidence."
