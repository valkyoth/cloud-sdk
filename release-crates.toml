[release]
version = "0.46.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.45.0"
version = "0.46.0"
change = "code"
publish = true
reason = "Add endpoint-bound request fingerprints, borrowed idempotency guards, and one-use bounded retry permits."

[crates."cloud-sdk-hetzner"]
previous_version = "0.35.0"
version = "0.36.0"
change = "code"
publish = true
reason = "Source-lock immutable prepared request bodies as byte-for-byte replayable."

[crates."cloud-sdk-reqwest"]
previous_version = "0.31.0"
version = "0.31.1"
change = "dependency"
publish = true
reason = "Update cloud-sdk to v0.46 and the reviewed transitive ipnet patch without adding transport-owned retry."

[crates."cloud-sdk-sanitization"]
previous_version = "0.16.0"
version = "0.16.0"
change = "unchanged"
publish = false
reason = "No code, dependency, or package metadata changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.25.2"
version = "0.26.0"
change = "code"
publish = true
reason = "Record prepared request body replayability for provider-neutral policy assertions."
