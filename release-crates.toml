[release]
version = "0.85.0"
milestone = "0.85.0"
baseline = "0.80.0"
review_baseline = "0.84.0"
cumulative_milestones = ["0.81.0", "0.82.0", "0.83.0", "0.84.0", "0.85.0"]
policy = "independent"
stage = "public"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "0.80.0"
version = "0.85.0"
change = "code"
publish = true
reason = "Publish the cumulative provider-neutral v0.81-v0.85 checkpoint."

[crates."cloud-sdk-hetzner"]
previous_version = "0.43.0"
version = "0.44.0"
change = "code"
publish = true
reason = "Publish cumulative Robot subnet, reset, failover, Wake-on-LAN, and boot management."

[crates."cloud-sdk-reqwest"]
previous_version = "0.35.1"
version = "0.35.2"
change = "dependency"
publish = true
reason = "Require the v0.85 provider-neutral core without changing transport behavior."

[crates."cloud-sdk-sanitization"]
previous_version = "0.19.0"
version = "0.19.0"
change = "unchanged"
publish = false
reason = "No sanitization boundary changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.30.3"
version = "0.30.4"
change = "dependency"
publish = true
reason = "Require the v0.85 provider-neutral core without changing testkit behavior."
