[release]
version = "0.22.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.21.0"
version = "0.22.0"
change = "code"
publish = true
reason = "Isolated fuzz harness, deterministic adversarial tests, and v0.22 release evidence."

[crates."cloud-sdk-hetzner"]
previous_version = "0.18.0"
version = "0.19.0"
change = "code"
publish = true
reason = "Adversarial Serde response tests for malformed and oversized upstream inputs."

[crates."cloud-sdk-reqwest"]
previous_version = "0.15.3"
version = "0.15.4"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.22 facade line."

[crates."cloud-sdk-sanitization"]
previous_version = "0.13.7"
version = "0.13.8"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.22 facade line."

[crates."cloud-sdk-testkit"]
previous_version = "0.15.3"
version = "0.15.4"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.22 facade line."
