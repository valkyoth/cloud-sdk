[release]
version = "1.1.0"
milestone = "1.1.0"
baseline = "1.0.0"
review_baseline = "1.0.0"
cumulative_milestones = ["1.1.0"]
policy = "independent"
stage = "public"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "1.0.0"
version = "1.1.0"
change = "code"
publish = true
reason = "Add shared incremental decoding and transport user-agent identity; retain empty defaults."

[crates."cloud-sdk-cratesio"]
previous_version = "none"
version = "1.1.0"
change = "code"
publish = true
reason = "Release all 51 source-locked crates.io operations with three-mode clients and Cargo-compatible workflows."

[crates."cloud-sdk-hetzner"]
previous_version = "1.0.0"
version = "1.1.0"
change = "code"
publish = true
reason = "Refresh reviewed Hetzner schemas, add Network members, and preserve announced deprecation compatibility."

[crates."cloud-sdk-reqwest"]
previous_version = "1.0.0"
version = "1.1.0"
change = "code"
publish = true
reason = "Add bounded streaming upload/download execution, user-agent identity and DNS shutdown hardening."

[crates."cloud-sdk-sanitization"]
previous_version = "1.0.0"
version = "1.1.0"
change = "metadata"
publish = true
reason = "Publish synchronized 1.1.0 documentation and reviewed dependency metadata; no first-party runtime change."

[crates."cloud-sdk-testkit"]
previous_version = "1.0.0"
version = "1.1.0"
change = "metadata"
publish = true
reason = "Publish synchronized 1.1.0 documentation and workspace dependencies; no first-party runtime change."
