[release]
version = "0.37.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.36.0"
version = "0.37.0"
change = "code"
publish = true
reason = "Seal response provenance and return cleanup-owning checked guards."

[crates."cloud-sdk-hetzner"]
previous_version = "0.29.0"
version = "0.30.0"
change = "code"
publish = true
reason = "Decode only from admitted cleanup-owning response buffers."

[crates."cloud-sdk-reqwest"]
previous_version = "0.24.0"
version = "0.25.0"
change = "code"
publish = true
reason = "Write blocking and async responses through the sealed core writer."

[crates."cloud-sdk-sanitization"]
previous_version = "0.15.4"
version = "0.15.5"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.37 facade line."

[crates."cloud-sdk-testkit"]
previous_version = "0.21.0"
version = "0.22.0"
change = "code"
publish = true
reason = "Exercise deterministic fixtures through the sealed response writer."
