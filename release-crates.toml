[release]
version = "0.29.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.28.0"
version = "0.29.0"
change = "code"
publish = true
reason = "Add prepared request metadata, bounded execution, and checked response policy."

[crates."cloud-sdk-hetzner"]
previous_version = "0.22.0"
version = "0.22.1"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.29 facade line."

[crates."cloud-sdk-reqwest"]
previous_version = "0.19.0"
version = "0.20.0"
change = "code"
publish = true
reason = "Capture validated response content types and reject malformed or duplicate values."

[crates."cloud-sdk-sanitization"]
previous_version = "0.13.14"
version = "0.13.15"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.29 facade line."

[crates."cloud-sdk-testkit"]
previous_version = "0.17.0"
version = "0.18.0"
change = "code"
publish = true
reason = "Add prepared-request records, bound endpoints, and response content-type fixtures."
