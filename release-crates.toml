[release]
version = "0.48.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.47.0"
version = "0.48.0"
change = "code"
publish = true
reason = "Add bounded blocking, Send-async, and local-async streaming policy, accounting, replay, cleanup, and drivers."

[crates."cloud-sdk-hetzner"]
previous_version = "0.36.1"
version = "0.36.2"
change = "dependency"
publish = true
reason = "Update cloud-sdk to v0.48 without changing provider request or response behavior."

[crates."cloud-sdk-reqwest"]
previous_version = "0.32.0"
version = "0.32.1"
change = "dependency"
publish = true
reason = "Update cloud-sdk to v0.48 without changing reqwest adapter behavior."

[crates."cloud-sdk-sanitization"]
previous_version = "0.16.0"
version = "0.16.0"
change = "unchanged"
publish = false
reason = "No code, dependency, or package metadata changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.27.0"
version = "0.28.0"
change = "code"
publish = true
reason = "Add deterministic bounded streaming source and short-write sink fixtures."
