[release]
version = "0.100.0"
milestone = "0.100.0"
baseline = "0.95.0"
review_baseline = "0.99.0"
cumulative_milestones = ["0.96.0", "0.97.0", "0.98.0", "0.99.0", "0.100.0"]
policy = "independent"
stage = "public"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "0.95.0"
version = "0.100.0"
change = "code"
publish = true
reason = "Publish the cumulative v0.96-v0.100 final development checkpoint."

[crates."cloud-sdk-hetzner"]
previous_version = "0.46.0"
version = "0.47.0"
change = "code"
publish = true
reason = "Publish accumulated request fidelity, metadata, and controlled-mutation qualification."

[crates."cloud-sdk-reqwest"]
previous_version = "0.36.0"
version = "0.37.0"
change = "code"
publish = true
reason = "Publish accumulated target support, diagnostics, and mutation transport qualification."

[crates."cloud-sdk-sanitization"]
previous_version = "0.19.0"
version = "0.19.0"
change = "unchanged"
publish = false
reason = "No sanitization boundary changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.31.0"
version = "0.31.1"
change = "dependency"
publish = true
reason = "Publish the cumulative cloud-sdk dependency update."
