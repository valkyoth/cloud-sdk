[release]
version = "0.69.0"
milestone = "0.69.0"
baseline = "0.65.0"
review_baseline = "0.68.0"
cumulative_milestones = ["0.66.0", "0.67.0", "0.68.0", "0.69.0"]
policy = "independent"
stage = "internal"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "0.68.0"
version = "0.69.0"
change = "code"
publish = false
reason = "Add complete client workspace capacity profiles and owned storage."

[crates."cloud-sdk-hetzner"]
previous_version = "0.40.0"
version = "0.40.0"
change = "code"
publish = false
reason = "Accumulate the service-typed Hetzner client construction and read-only execution foundation for v0.70."

[crates."cloud-sdk-reqwest"]
previous_version = "0.34.0"
version = "0.34.0"
change = "unchanged"
publish = false
reason = "No transport boundary changes."

[crates."cloud-sdk-sanitization"]
previous_version = "0.18.0"
version = "0.18.0"
change = "unchanged"
publish = false
reason = "No sanitization boundary changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.30.0"
version = "0.30.0"
change = "unchanged"
publish = false
reason = "No testkit boundary changes."
