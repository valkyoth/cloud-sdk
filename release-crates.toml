[release]
version = "0.21.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.20.0"
version = "0.21.0"
change = "code"
publish = true
reason = "Compile-checked quickstart, documentation gates, and v0.21 release evidence."

[crates."cloud-sdk-hetzner"]
previous_version = "0.17.1"
version = "0.18.0"
change = "code"
publish = true
reason = "Compile-checked Hetzner read-only, mutation, pagination, action, DNS, and Storage Box examples."

[crates."cloud-sdk-reqwest"]
previous_version = "0.15.2"
version = "0.15.3"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.21 facade line."

[crates."cloud-sdk-sanitization"]
previous_version = "0.13.6"
version = "0.13.7"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.21 facade line."

[crates."cloud-sdk-testkit"]
previous_version = "0.15.2"
version = "0.15.3"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.21 facade line."
