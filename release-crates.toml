[release]
version = "1.1.0"
milestone = "1.1.0"
baseline = "1.0.0"
review_baseline = "1.0.0"
cumulative_milestones = ["1.1.0"]
policy = "independent"
stage = "candidate"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "1.0.0"
version = "1.1.0"
change = "code"
publish = false
reason = "Add shared incremental decoding and transport user-agent identity without enabling candidate publication."

[crates."cloud-sdk-cratesio"]
previous_version = "none"
version = "1.1.0"
change = "code"
publish = false
reason = "Add the initial crates.io provider boundary without enabling candidate publication."

[crates."cloud-sdk-hetzner"]
previous_version = "1.0.0"
version = "1.1.0"
change = "code"
publish = false
reason = "Refresh reviewed Hetzner schemas and announced deprecation compatibility; publication remains disabled."

[crates."cloud-sdk-reqwest"]
previous_version = "1.0.0"
version = "1.1.0"
change = "code"
publish = false
reason = "Expose stable identifying user-agent configuration on neutral raw transports; publication remains disabled."

[crates."cloud-sdk-sanitization"]
previous_version = "1.0.0"
version = "1.1.0"
change = "metadata"
publish = false
reason = "Keep the cleanup boundary on the coherent unreleased workspace version."

[crates."cloud-sdk-testkit"]
previous_version = "1.0.0"
version = "1.1.0"
change = "metadata"
publish = false
reason = "Keep the testkit on the coherent unreleased workspace version."
