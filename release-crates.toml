[release]
version = "0.12.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.11.0"
version = "0.12.0"
change = "metadata"
publish = true
reason = "README updates and v0.12.0 release evidence for the provider-neutral facade."

[crates."cloud-sdk-hetzner"]
previous_version = "0.11.0"
version = "0.12.0"
change = "code"
publish = true
reason = "No_std DNS Zone CRUD, zonefile, nameserver, TTL, protection, and action request domains."

[crates."cloud-sdk-reqwest"]
previous_version = "none"
version = "0.12.0"
change = "code"
publish = true
reason = "Initial provider-neutral no_std reqwest transport boundary for all cloud providers."

[crates."cloud-sdk-sanitization"]
previous_version = "none"
version = "0.12.0"
change = "code"
publish = true
reason = "Initial provider-neutral no_std sanitization boundary for reusable cloud SDK secret helpers."

[crates."cloud-sdk-testkit"]
previous_version = "none"
version = "0.12.0"
change = "code"
publish = true
reason = "Initial provider-neutral no_std mock transport and fixture boundary for all cloud providers."
