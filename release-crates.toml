[release]
version = "0.47.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.46.0"
version = "0.47.0"
change = "code"
publish = true
reason = "Add local async transport, authenticated execution, raw execution, prepared request, pagination, and retry contracts."

[crates."cloud-sdk-hetzner"]
previous_version = "0.36.0"
version = "0.36.1"
change = "dependency"
publish = true
reason = "Update cloud-sdk to v0.47 without changing provider request or response behavior."

[crates."cloud-sdk-reqwest"]
previous_version = "0.31.1"
version = "0.32.0"
change = "code"
publish = true
reason = "Migrate Send async transports to non-committing staging and SDK-owned response commitment."

[crates."cloud-sdk-sanitization"]
previous_version = "0.16.0"
version = "0.16.0"
change = "unchanged"
publish = false
reason = "No code, dependency, or package metadata changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.26.0"
version = "0.27.0"
change = "code"
publish = true
reason = "Add a no-allocation local-only mock transport and local async conformance fixtures."
