[release]
version = "0.36.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.35.0"
version = "0.36.0"
change = "code"
publish = true
reason = "Add bounded ordered request and response header contracts."

[crates."cloud-sdk-hetzner"]
previous_version = "0.28.0"
version = "0.29.0"
change = "code"
publish = true
reason = "Move Hetzner Accept and Content-Type policy into prepared requests."

[crates."cloud-sdk-reqwest"]
previous_version = "0.23.0"
version = "0.24.0"
change = "code"
publish = true
reason = "Forward admitted request headers and retain bounded response metadata."

[crates."cloud-sdk-sanitization"]
previous_version = "0.15.3"
version = "0.15.4"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.36 facade line."

[crates."cloud-sdk-testkit"]
previous_version = "0.20.0"
version = "0.21.0"
change = "code"
publish = true
reason = "Match exact request headers and complete bounded response metadata."
