[release]
version = "0.2.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.1.0"
version = "0.2.0"
change = "metadata"
publish = true
reason = "Source-lock documentation, release metadata, API drift evidence, and v0.2.0 release gate."

[crates."cloud-sdk-hetzner"]
previous_version = "0.1.0"
version = "0.2.0"
change = "metadata"
publish = true
reason = "Source-locked Hetzner Cloud/DNS and Storage Box API matrix with drift fingerprints."

[crates."cloud-sdk-hetzner-reqwest"]
previous_version = "0.1.0"
version = "0.2.0"
change = "metadata"
publish = true
reason = "Keep optional transport boundary metadata aligned with v0.2.0 release evidence."

[crates."cloud-sdk-hetzner-sanitization"]
previous_version = "0.1.0"
version = "0.2.0"
change = "metadata"
publish = true
reason = "Keep sanitization boundary metadata aligned with v0.2.0 release evidence."

[crates."cloud-sdk-hetzner-testkit"]
previous_version = "0.1.0"
version = "0.2.0"
change = "metadata"
publish = true
reason = "Keep testkit boundary metadata aligned with v0.2.0 release evidence."
