[release]
version = "0.43.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.42.0"
version = "0.43.0"
change = "code"
publish = true
reason = "Require complete authentication and raw response policies on prepared execution."

[crates."cloud-sdk-hetzner"]
previous_version = "0.32.3"
version = "0.33.0"
change = "code"
publish = true
reason = "Migrate all 208 active operations to exact service, authentication, and raw wire policy."

[crates."cloud-sdk-reqwest"]
previous_version = "0.29.0"
version = "0.30.0"
change = "code"
publish = true
reason = "Execute bearer and Basic authentication through the bounded raw Hyper engine with delivery phases."

[crates."cloud-sdk-sanitization"]
previous_version = "0.16.0"
version = "0.16.0"
change = "unchanged"
publish = false
reason = "No code, dependency, or package metadata changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.24.2"
version = "0.25.0"
change = "code"
publish = true
reason = "Mock authenticated prepared execution and expose complete auth and raw response policies."
