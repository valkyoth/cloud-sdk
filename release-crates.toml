[release]
version = "0.39.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.38.0"
version = "0.39.0"
change = "code"
publish = true
reason = "Add transactional snapshot encoders and guarded capacity profiles."

[crates."cloud-sdk-hetzner"]
previous_version = "0.31.0"
version = "0.32.0"
change = "code"
publish = true
reason = "Migrate request paths, queries, and JSON bodies to atomic encoding."

[crates."cloud-sdk-reqwest"]
previous_version = "0.26.0"
version = "0.26.1"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.39 facade line."

[crates."cloud-sdk-sanitization"]
previous_version = "0.16.0"
version = "0.16.0"
change = "unchanged"
publish = false
reason = "No code, dependency, or package metadata changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.23.0"
version = "0.23.1"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.39 facade line."
