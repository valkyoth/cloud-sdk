[release]
version = "0.50.0"
milestone = "0.50.0"
baseline = "0.50.0"
cumulative_milestones = []
policy = "independent"
stage = "public"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "0.49.0"
version = "0.50.0"
change = "code"
publish = true
reason = "Add compile-time operation identifier construction for provider associations."

[crates."cloud-sdk-hetzner"]
previous_version = "0.37.0"
version = "0.38.0"
change = "code"
publish = true
reason = "Add exhaustive compile-time associations for all 208 active Hetzner operations."

[crates."cloud-sdk-reqwest"]
previous_version = "0.32.2"
version = "0.32.3"
change = "dependency"
publish = true
reason = "Update cloud-sdk to v0.50 without changing reqwest adapter behavior."

[crates."cloud-sdk-sanitization"]
previous_version = "0.17.0"
version = "0.17.0"
change = "unchanged"
publish = false
reason = "No code, dependency, or package metadata changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.28.1"
version = "0.28.2"
change = "dependency"
publish = true
reason = "Update cloud-sdk to v0.50 without changing testkit behavior."
