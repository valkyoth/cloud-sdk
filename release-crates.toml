[release]
version = "0.38.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.37.0"
version = "0.38.0"
change = "code"
publish = true
reason = "Make response cleanup mandatory and own all checked decode workspace."

[crates."cloud-sdk-hetzner"]
previous_version = "0.30.0"
version = "0.31.0"
change = "code"
publish = true
reason = "Use core-owned decoder scratch and mandatory response cleanup."

[crates."cloud-sdk-reqwest"]
previous_version = "0.25.0"
version = "0.26.0"
change = "code"
publish = true
reason = "Transfer non-Copy response metadata into mandatory cleanup owners."

[crates."cloud-sdk-sanitization"]
previous_version = "0.15.5"
version = "0.16.0"
change = "code"
publish = true
reason = "Invert the cleanup dependency and expose the audited scalar sanitizer."

[crates."cloud-sdk-testkit"]
previous_version = "0.22.0"
version = "0.23.0"
change = "code"
publish = true
reason = "Move fixtures through non-Copy metadata and mandatory response cleanup."
