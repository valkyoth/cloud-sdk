[release]
version = "0.93.0"
milestone = "0.93.0"
baseline = "0.90.0"
review_baseline = "0.92.0"
cumulative_milestones = ["0.91.0", "0.92.0", "0.93.0"]
policy = "independent"
stage = "internal"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "0.92.0"
version = "0.93.0"
change = "code"
publish = false
reason = "Advance the internal Robot billable-order milestone; defer publication to v0.95.0."

[crates."cloud-sdk-hetzner"]
previous_version = "0.45.0"
version = "0.45.0"
change = "code"
publish = false
reason = "Accumulate source-locked Robot ordering catalogs, transactions, and guarded mutations for v0.95.0."

[crates."cloud-sdk-reqwest"]
previous_version = "0.35.3"
version = "0.35.3"
change = "unchanged"
publish = false
reason = "No reqwest transport boundary changes."

[crates."cloud-sdk-sanitization"]
previous_version = "0.19.0"
version = "0.19.0"
change = "unchanged"
publish = false
reason = "No sanitization boundary changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.30.5"
version = "0.30.5"
change = "unchanged"
publish = false
reason = "No testkit boundary changes."
