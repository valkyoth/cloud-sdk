[release]
version = "0.65.0"
milestone = "0.65.0"
baseline = "0.60.0"
review_baseline = "0.64.0"
cumulative_milestones = ["0.61.0", "0.62.0", "0.63.0", "0.64.0", "0.65.0"]
policy = "independent"
stage = "public"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "0.60.0"
version = "0.65.0"
change = "code"
publish = true
reason = "Publish the cumulative provider-neutral contract and v0.65 release checkpoint."

[crates."cloud-sdk-hetzner"]
previous_version = "0.39.1"
version = "0.40.0"
change = "code"
publish = true
reason = "Publish cumulative source-complete Cloud and DNS response models."

[crates."cloud-sdk-reqwest"]
previous_version = "0.33.0"
version = "0.34.0"
change = "code"
publish = true
reason = "Publish cumulative transport hardening and the exact base64-ng 2.0.1 update."

[crates."cloud-sdk-sanitization"]
previous_version = "0.18.0"
version = "0.18.0"
change = "unchanged"
publish = false
reason = "No sanitization boundary changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.29.1"
version = "0.30.0"
change = "code"
publish = true
reason = "Publish cumulative exact-response and permit fixture improvements."
