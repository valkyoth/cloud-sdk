[release]
version = "0.57.0"
milestone = "0.57.0"
baseline = "0.55.0"
review_baseline = "0.56.0"
cumulative_milestones = ["0.56.0", "0.57.0"]
policy = "independent"
stage = "internal"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "0.56.0"
version = "0.57.0"
change = "metadata"
publish = false
reason = "Record the unpublished OVHcloud API v2 source-lock probe without crates.io publication."

[crates."cloud-sdk-hetzner"]
previous_version = "0.39.0"
version = "0.39.0"
change = "unchanged"
publish = false
reason = "No provider crate source or package metadata changes."

[crates."cloud-sdk-reqwest"]
previous_version = "0.32.4"
version = "0.32.4"
change = "unchanged"
publish = false
reason = "No adapter source, dependency, or package metadata changes."

[crates."cloud-sdk-sanitization"]
previous_version = "0.18.0"
version = "0.18.0"
change = "unchanged"
publish = false
reason = "No sanitization boundary changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.29.0"
version = "0.29.0"
change = "unchanged"
publish = false
reason = "No testkit source or package metadata changes."
