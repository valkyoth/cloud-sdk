[release]
version = "0.20.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.19.0"
version = "0.20.0"
change = "metadata"
publish = true
reason = "Platform support documentation, release evidence, and v0.20 facade metadata."

[crates."cloud-sdk-hetzner"]
previous_version = "0.17.0"
version = "0.17.1"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.20 facade line."

[crates."cloud-sdk-reqwest"]
previous_version = "0.15.1"
version = "0.15.2"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.20 facade line."

[crates."cloud-sdk-sanitization"]
previous_version = "0.13.5"
version = "0.13.6"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.20 facade line."

[crates."cloud-sdk-testkit"]
previous_version = "0.15.1"
version = "0.15.2"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.20 facade line."
