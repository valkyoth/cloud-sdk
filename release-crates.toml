[release]
version = "0.40.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.39.0"
version = "0.40.0"
change = "code"
publish = true
reason = "Add delivery-phased raw HTTP execution and bounded response-wire policy."

[crates."cloud-sdk-hetzner"]
previous_version = "0.32.0"
version = "0.32.1"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.40 facade line."

[crates."cloud-sdk-reqwest"]
previous_version = "0.26.1"
version = "0.27.0"
change = "code"
publish = true
reason = "Add raw blocking, async, deterministic-root, and FIPS HTTP executors."

[crates."cloud-sdk-sanitization"]
previous_version = "0.16.0"
version = "0.16.0"
change = "unchanged"
publish = false
reason = "No code, dependency, or package metadata changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.23.1"
version = "0.24.0"
change = "code"
publish = true
reason = "Add deterministic delivery-phase fault injection for raw executors."
