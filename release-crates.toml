[release]
version = "0.97.0"
milestone = "0.97.0"
baseline = "0.95.0"
review_baseline = "0.96.0"
cumulative_milestones = ["0.96.0", "0.97.0"]
policy = "independent"
stage = "internal"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "0.96.0"
version = "0.97.0"
change = "code"
publish = false
reason = "Advance the internal Hetzner scope-closure milestone; defer publication to v0.100.0."

[crates."cloud-sdk-hetzner"]
previous_version = "0.46.0"
version = "0.46.0"
change = "code"
publish = false
reason = "Accumulate canonical Server Metadata and changelog response validation for v0.100.0."

[crates."cloud-sdk-reqwest"]
previous_version = "0.36.0"
version = "0.36.0"
change = "code"
publish = false
reason = "Accumulate the credential-free exact link-local raw transport constructor for v0.100.0."

[crates."cloud-sdk-sanitization"]
previous_version = "0.19.0"
version = "0.19.0"
change = "unchanged"
publish = false
reason = "No sanitization boundary changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.31.0"
version = "0.31.0"
change = "unchanged"
publish = false
reason = "No testkit boundary changes."
