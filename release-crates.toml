[release]
version = "0.31.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.30.0"
version = "0.31.0"
change = "code"
publish = true
reason = "Bind validated operation identifiers and expose prepared response-policy validation."

[crates."cloud-sdk-hetzner"]
previous_version = "0.23.0"
version = "0.24.0"
change = "code"
publish = true
reason = "Decode every active operation through source-locked checked success and error models."

[crates."cloud-sdk-reqwest"]
previous_version = "0.20.1"
version = "0.20.2"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.31 facade line."

[crates."cloud-sdk-sanitization"]
previous_version = "0.13.16"
version = "0.13.17"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.31 facade line."

[crates."cloud-sdk-testkit"]
previous_version = "0.18.1"
version = "0.18.2"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.31 facade line."
