[release]
version = "0.70.0"
milestone = "0.70.0"
baseline = "0.65.0"
review_baseline = "0.69.0"
cumulative_milestones = ["0.66.0", "0.67.0", "0.68.0", "0.69.0", "0.70.0"]
policy = "independent"
stage = "public"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "0.65.0"
version = "0.70.0"
change = "code"
publish = true
reason = "Publish the cumulative provider-neutral v0.66-v0.70 checkpoint."

[crates."cloud-sdk-hetzner"]
previous_version = "0.40.0"
version = "0.41.0"
change = "code"
publish = true
reason = "Publish cumulative response models, operation bindings, client foundation, and complete Cloud client methods."

[crates."cloud-sdk-reqwest"]
previous_version = "0.34.0"
version = "0.34.1"
change = "dependency"
publish = true
reason = "Update the required cloud-sdk version for the public checkpoint."

[crates."cloud-sdk-sanitization"]
previous_version = "0.18.0"
version = "0.18.0"
change = "unchanged"
publish = false
reason = "No sanitization boundary changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.30.0"
version = "0.30.1"
change = "dependency"
publish = true
reason = "Update the required cloud-sdk version for the public checkpoint."
