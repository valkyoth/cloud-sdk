[release]
version = "0.34.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.33.0"
version = "0.34.0"
change = "code"
publish = true
reason = "Add provider-owned endpoint policy algebra and canonical authority identities."

[crates."cloud-sdk-hetzner"]
previous_version = "0.26.0"
version = "0.27.0"
change = "code"
publish = true
reason = "Own official fixed and finite endpoint policies for prepared Hetzner operations."

[crates."cloud-sdk-reqwest"]
previous_version = "0.21.0"
version = "0.22.0"
change = "code"
publish = true
reason = "Require provider policy or explicit acknowledgement for credential destinations."

[crates."cloud-sdk-sanitization"]
previous_version = "0.15.1"
version = "0.15.2"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.34 facade line."

[crates."cloud-sdk-testkit"]
previous_version = "0.18.4"
version = "0.19.0"
change = "code"
publish = true
reason = "Record non-static endpoint policies and verify prepared policy matching."
