[release]
version = "0.18.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.17.0"
version = "0.18.0"
change = "code"
publish = true
reason = "Provider-neutral pagination, action polling, and rate-limit metadata contracts."

[crates."cloud-sdk-hetzner"]
previous_version = "0.15.2"
version = "0.16.0"
change = "code"
publish = true
reason = "Strict Hetzner pagination metadata parsing and action polling conversion."

[crates."cloud-sdk-reqwest"]
previous_version = "0.14.0"
version = "0.15.0"
change = "code"
publish = true
reason = "Validate and propagate provider rate-limit response headers."

[crates."cloud-sdk-sanitization"]
previous_version = "0.13.3"
version = "0.13.4"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.18 facade line."

[crates."cloud-sdk-testkit"]
previous_version = "0.14.0"
version = "0.15.0"
change = "code"
publish = true
reason = "Propagate validated rate-limit metadata through deterministic fixtures."
