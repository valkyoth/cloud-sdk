[release]
version = "0.72.0"
milestone = "0.72.0"
baseline = "0.70.0"
review_baseline = "0.71.0"
cumulative_milestones = ["0.71.0", "0.72.0"]
policy = "independent"
stage = "internal"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "0.71.0"
version = "0.72.0"
change = "metadata"
publish = false
reason = "Record the complete Hetzner Security client milestone."

[crates."cloud-sdk-hetzner"]
previous_version = "0.41.0"
version = "0.41.0"
change = "code"
publish = false
reason = "Accumulate complete DNS and Security client methods for the v0.75 public checkpoint."

[crates."cloud-sdk-reqwest"]
previous_version = "0.34.1"
version = "0.34.1"
change = "unchanged"
publish = false
reason = "No transport boundary changes."

[crates."cloud-sdk-sanitization"]
previous_version = "0.18.0"
version = "0.18.0"
change = "unchanged"
publish = false
reason = "No sanitization boundary changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.30.1"
version = "0.30.1"
change = "unchanged"
publish = false
reason = "No testkit boundary changes."
