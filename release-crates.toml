[release]
version = "0.49.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.48.0"
version = "0.49.0"
change = "metadata"
publish = true
reason = "Publish the v0.49 facade documentation and provider version alignment."

[crates."cloud-sdk-hetzner"]
previous_version = "0.36.2"
version = "0.37.0"
change = "code"
publish = true
reason = "Add bounded incremental provider JSON decoding across arbitrary input chunks."

[crates."cloud-sdk-reqwest"]
previous_version = "0.32.1"
version = "0.32.2"
change = "dependency"
publish = true
reason = "Update cloud-sdk to v0.49 without changing reqwest adapter behavior."

[crates."cloud-sdk-sanitization"]
previous_version = "0.16.0"
version = "0.17.0"
change = "code"
publish = true
reason = "Add bounded fallible growth for protected UTF-8 parser staging."

[crates."cloud-sdk-testkit"]
previous_version = "0.28.0"
version = "0.28.1"
change = "dependency"
publish = true
reason = "Update cloud-sdk to v0.49 without changing testkit behavior."
