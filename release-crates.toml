[release]
version = "0.62.0"
milestone = "0.62.0"
baseline = "0.60.0"
review_baseline = "0.61.0"
cumulative_milestones = ["0.61.0", "0.62.0"]
policy = "independent"
stage = "internal"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "0.61.0"
version = "0.62.0"
change = "code"
publish = false
reason = "Freeze provider-neutral contracts after cross-provider and full-fidelity Hetzner evidence."

[crates."cloud-sdk-hetzner"]
previous_version = "0.39.1"
version = "0.39.1"
change = "code"
publish = false
reason = "Accumulate source-complete Hetzner vertical models and exact service bindings for v0.65."

[crates."cloud-sdk-reqwest"]
previous_version = "0.33.0"
version = "0.33.0"
change = "dependency"
publish = false
reason = "Accumulate the source core dependency for the v0.65 public checkpoint."

[crates."cloud-sdk-sanitization"]
previous_version = "0.18.0"
version = "0.18.0"
change = "unchanged"
publish = false
reason = "No sanitization boundary changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.29.1"
version = "0.29.1"
change = "code"
publish = false
reason = "Accumulate exact success statuses and delivery-classified permit fixtures for v0.65."
