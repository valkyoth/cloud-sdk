[release]
version = "0.61.0"
milestone = "0.61.0"
baseline = "0.60.0"
review_baseline = "0.60.0"
cumulative_milestones = ["0.61.0"]
policy = "independent"
stage = "internal"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "0.60.0"
version = "0.61.0"
change = "metadata"
publish = false
reason = "Record the unpublished OVHcloud end-to-end execution probe and v0.61 release evidence."

[crates."cloud-sdk-hetzner"]
previous_version = "0.39.1"
version = "0.39.1"
change = "dependency"
publish = false
reason = "Accumulate the source core dependency for the v0.65 public checkpoint."

[crates."cloud-sdk-reqwest"]
previous_version = "0.33.0"
version = "0.33.0"
change = "dependency"
publish = false
reason = "Accumulate the source core dependency for the v0.65 public checkpoint."

[crates."cloud-sdk-sanitization"]
previous_version = "0.18.0"
version = "0.18.0"
change = "unchanged"
publish = false
reason = "No sanitization boundary changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.29.1"
version = "0.29.1"
change = "dependency"
publish = false
reason = "Accumulate the source core dependency for the v0.65 public checkpoint."
