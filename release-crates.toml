[release]
version = "0.3.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.2.0"
version = "0.3.0"
change = "code"
publish = true
reason = "Provider-neutral method token helper, crate-local documentation, release metadata, and v0.3.0 release gate evidence."

[crates."cloud-sdk-hetzner"]
previous_version = "0.2.0"
version = "0.3.0"
change = "code"
publish = true
reason = "Core no_std request/response policy domains for endpoint paths, query encoding, labels, pagination, actions, errors, and rate-limit metadata."

[crates."cloud-sdk-hetzner-reqwest"]
previous_version = "0.2.0"
version = "0.3.0"
change = "metadata"
publish = true
reason = "Publish crate-local README and rustdoc metadata aligned with v0.3.0 release evidence."

[crates."cloud-sdk-hetzner-sanitization"]
previous_version = "0.2.0"
version = "0.3.0"
change = "metadata"
publish = true
reason = "Publish crate-local README and rustdoc metadata aligned with v0.3.0 release evidence."

[crates."cloud-sdk-hetzner-testkit"]
previous_version = "0.2.0"
version = "0.3.0"
change = "metadata"
publish = true
reason = "Publish crate-local README and rustdoc metadata aligned with v0.3.0 release evidence."
