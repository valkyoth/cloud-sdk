[release]
version = "0.4.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.3.0"
version = "0.4.0"
change = "code"
publish = true
reason = "Release metadata, README updates, and v0.4.0 release gate evidence for the provider-neutral facade."

[crates."cloud-sdk-hetzner"]
previous_version = "0.3.0"
version = "0.4.0"
change = "code"
publish = true
reason = "Read-only no_std catalog request domains for locations, pricing, server types, load balancer types, ISOs, and public images."

[crates."cloud-sdk-hetzner-reqwest"]
previous_version = "0.3.0"
version = "0.4.0"
change = "metadata"
publish = true
reason = "Keep optional transport boundary metadata aligned with v0.4.0 release evidence."

[crates."cloud-sdk-hetzner-sanitization"]
previous_version = "0.3.0"
version = "0.4.0"
change = "metadata"
publish = true
reason = "Keep sanitization boundary metadata aligned with v0.4.0 release evidence."

[crates."cloud-sdk-hetzner-testkit"]
previous_version = "0.3.0"
version = "0.4.0"
change = "metadata"
publish = true
reason = "Keep testkit boundary metadata aligned with v0.4.0 release evidence."
