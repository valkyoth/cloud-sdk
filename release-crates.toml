[release]
version = "0.24.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.23.0"
version = "0.24.0"
change = "metadata"
publish = true
reason = "Dependency, tooling, deterministic-root transport documentation, and v0.24 release evidence."

[crates."cloud-sdk-hetzner"]
previous_version = "0.19.1"
version = "0.19.2"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.24 facade line."

[crates."cloud-sdk-reqwest"]
previous_version = "0.16.0"
version = "0.17.0"
change = "code"
publish = true
reason = "Optional deterministic Mozilla-root transport and hardened dependency/tooling evidence."

[crates."cloud-sdk-sanitization"]
previous_version = "0.13.9"
version = "0.13.10"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.24 facade line."

[crates."cloud-sdk-testkit"]
previous_version = "0.15.5"
version = "0.15.6"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.24 facade line."
