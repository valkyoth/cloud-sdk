[release]
version = "0.44.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.43.0"
version = "0.44.0"
change = "code"
publish = true
reason = "Add bounded numbered, offset, cursor, marker, and provider-link pagination strategies."

[crates."cloud-sdk-hetzner"]
previous_version = "0.33.0"
version = "0.34.0"
change = "code"
publish = true
reason = "Migrate Hetzner pagination metadata to the numbered strategy contract."

[crates."cloud-sdk-reqwest"]
previous_version = "0.30.0"
version = "0.30.1"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.44 facade line."

[crates."cloud-sdk-sanitization"]
previous_version = "0.16.0"
version = "0.16.0"
change = "unchanged"
publish = false
reason = "No code, dependency, or package metadata changes."

[crates."cloud-sdk-testkit"]
previous_version = "0.25.0"
version = "0.25.1"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.44 facade line."
