[release]
version = "0.33.0"
policy = "independent"

[crates."cloud-sdk"]
previous_version = "0.32.0"
version = "0.33.0"
change = "code"
publish = true
reason = "Add the complete bounded provider-neutral HTTP method domain."

[crates."cloud-sdk-hetzner"]
previous_version = "0.25.0"
version = "0.26.0"
change = "code"
publish = true
reason = "Declare operation impact, semantics, and retry eligibility independently from HTTP methods."

[crates."cloud-sdk-reqwest"]
previous_version = "0.20.3"
version = "0.21.0"
change = "code"
publish = true
reason = "Map all admitted known and extension methods through blocking and async transports."

[crates."cloud-sdk-sanitization"]
previous_version = "0.15.0"
version = "0.15.1"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency to the v0.33 facade line."

[crates."cloud-sdk-testkit"]
previous_version = "0.18.3"
version = "0.18.4"
change = "dependency"
publish = true
reason = "Update the cloud-sdk dependency and method-matching tests to the v0.33 facade line."
