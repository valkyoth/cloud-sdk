[release]
version = "1.0.0"
milestone = "1.0.0"
baseline = "0.100.0"
review_baseline = "0.100.0"
cumulative_milestones = ["1.0.0"]
policy = "independent"
stage = "public"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "0.100.0"
version = "1.0.0"
change = "metadata"
publish = true
reason = "Publish the qualified provider-neutral API with exact reviewed third-party requirements as stable 1.0."

[crates."cloud-sdk-hetzner"]
previous_version = "0.47.0"
version = "1.0.0"
change = "metadata"
publish = true
reason = "Publish the complete qualified Hetzner provider with exact reviewed third-party requirements as stable 1.0."

[crates."cloud-sdk-reqwest"]
previous_version = "0.37.0"
version = "1.0.0"
change = "metadata"
publish = true
reason = "Publish the qualified reqwest adapter with exact reviewed transport requirements as stable 1.0."

[crates."cloud-sdk-sanitization"]
previous_version = "0.19.0"
version = "1.0.0"
change = "metadata"
publish = true
reason = "Publish the qualified cleanup boundary with an exact reviewed sanitization requirement as stable 1.0."

[crates."cloud-sdk-testkit"]
previous_version = "0.31.1"
version = "1.0.0"
change = "metadata"
publish = true
reason = "Publish the qualified provider-neutral testkit as stable 1.0."
