[release]
version = "0.79.0"
milestone = "0.79.0"
baseline = "0.75.0"
review_baseline = "0.78.0"
cumulative_milestones = ["0.76.0", "0.77.0", "0.78.0", "0.79.0"]
policy = "independent"
stage = "internal"
exceptional = false
exception_reason = ""

[crates."cloud-sdk"]
previous_version = "0.78.0"
version = "0.79.0"
change = "metadata"
publish = false
reason = "Record the Robot cancellation milestone; defer publication to v0.80.0."

[crates."cloud-sdk-hetzner"]
previous_version = "0.42.0"
version = "0.42.0"
change = "code"
publish = false
reason = "Accumulate nine Robot cancellation operations and strict response models for v0.80.0."

[crates."cloud-sdk-reqwest"]
previous_version = "0.35.0"
version = "0.35.0"
change = "unchanged"
publish = false
reason = "No reqwest transport boundary changes."

[crates."cloud-sdk-sanitization"]
previous_version = "0.18.0"
version = "0.18.0"
change = "unchanged"
publish = false
reason = "No neutral sanitization boundary changes in v0.79.0."

[crates."cloud-sdk-testkit"]
previous_version = "0.30.2"
version = "0.30.2"
change = "unchanged"
publish = false
reason = "No testkit boundary changes."
