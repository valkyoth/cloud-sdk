# Contributing

Run the local gate before opening a pull request:

```bash
scripts/checks.sh
```

Security-sensitive changes need tests, documentation, and dependency-policy
evidence when new crates or tools are admitted.
